# Application Settings

model:
  name: "qwen3-vl:235b-instruct-cloud"
  provider: "ollama"
  temperature: 0.7
  max_tokens: 4096
  timeout: 120

ui:
  theme: "dark"
  primary_color: "#FF4B4B"
  font: "Inter"

video:
  max_frames: 10
  extraction_interval: 2 # Extract 1 frame every 2 seconds
